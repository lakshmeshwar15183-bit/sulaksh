# Dependencies
node_modules/

# Environment / secrets — never commit
.env

# Database files
*.db
*.db-journal
*.db-wal
*.db-shm
data/
# Keep the generated static materials snapshot (committed as an always-available fallback)
!assets/data/
!assets/data/*.json

# OS
.DS_Store

# Netlify
.netlify/